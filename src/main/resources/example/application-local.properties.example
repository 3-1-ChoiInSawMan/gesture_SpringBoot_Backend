# 로컬 DB 설정
spring.datasource.url=jdbc:mysql://localhost:3306/{DB명}
spring.datasource.username={DB유저명}
spring.datasource.password={DB비밀번호}

# JWT (RS256)
spring.jwt.private-key=classpath:keys/private_key.pem
spring.jwt.public-key=classpath:keys/public_key.pem
